import  './index.css';

import numeral from 'numeral';

const courseValue = numeral(666).format('$0.00');

debugger;

console.log(`I would pay ${courseValue} for this mantle young devil of hell haHAA.`);
